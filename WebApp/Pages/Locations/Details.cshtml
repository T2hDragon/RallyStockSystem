@page
@using Domain
@model WebApp.Pages_Locations.DetailsModel

@{
    ViewData["Title"] = Model.Location.Name + " Details";
}

<h1>Details</h1>

<div>
    <h4>Item Location</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Location Name
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Location.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Location.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
